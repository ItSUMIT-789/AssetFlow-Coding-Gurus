import { useState } from 'react';import { Link, useLocation, useNavigate } from 'react-router-dom';import { Mail } from 'lucide-react';import AuthLayout from '../components/AuthLayout';import {InputField,LoadingButton,PasswordField} from '../components/FormFields';import {validateLogin} from '../utils/validation'
export default function Login(){const [form,setForm]=useState({email:'',password:'',remember:false});const [errors,setErrors]=useState({});const [loading,setLoading]=useState(false);const navigate=useNavigate();const location=useLocation();const update=e=>setForm({...form,[e.target.name]:e.target.type==='checkbox'?e.target.checked:e.target.value});const submit=e=>{e.preventDefault();const next=validateLogin(form);setErrors(next);if(Object.keys(next).length)return;setLoading(true);setTimeout(()=>{localStorage.setItem('assetflow_auth','true');localStorage.setItem('assetflow_login_email',form.email);navigate(location.state?.from?.pathname||'/dashboard',{replace:true})},800)};return <AuthLayout title="Welcome Back" message="Securely access your organization’s asset management workspace and keep every resource under control."><form onSubmit={submit} className="rounded-3xl border border-slate-200 bg-white p-6 shadow-xl shadow-slate-200/60 sm:p-9"><p className="text-sm font-bold text-brand-500">WELCOME BACK</p><h2 className="mt-2 text-3xl font-extrabold text-navy-900">Sign in to AssetFlow</h2><p className="mt-2 text-sm text-slate-500">Enter your work credentials to continue.</p><div className="mt-8 space-y-5"><InputField label="Email address" name="email" value={form.email} onChange={update} error={errors.email} icon={Mail} placeholder="name@company.com" autoComplete="email"/><PasswordField label="Password" name="password" value={form.password} onChange={update} error={errors.password} placeholder="Enter your password" autoComplete="current-password"/></div><div className="my-5 flex items-center justify-between text-sm"><label className="flex items-center gap-2 text-slate-600"><input type="checkbox" name="remember" checked={form.remember} onChange={update} className="accent-brand-500"/> Remember me</label><a href="#" className="font-semibold text-brand-500 hover:underline">Forgot Password?</a></div><LoadingButton loading={loading}>Login</LoadingButton><p className="mt-6 text-center text-sm text-slate-500">New to AssetFlow? <Link to="/register" className="font-bold text-brand-500 hover:underline">Create an account</Link></p></form></AuthLayout>}
